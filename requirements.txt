# Core numerical
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
# Modeling scikit-learn==1.4.2 lightgbm==4.3.0
 # Optional: PyHealth (only needed if you use the real eICU loader variant)
 pyhealth==1.1.3
 # Tables / I/O utilities (if any script writes LaTeX)
 tabulate==0.9.0  # For clean CLI UX (optional)
 tqdm==4.66.4